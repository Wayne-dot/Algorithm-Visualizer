# Algorithm-Visualizer

A simple pathfinding visualizer. Open `index.html` in a browser, click on the grid to set the start (green) and end (red) points, then add walls. Choose an algorithm and press **Run** to see the path.
